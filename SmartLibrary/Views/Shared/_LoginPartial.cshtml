@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "d-flex ms-auto" }))
    {
        @Html.AntiForgeryToken()

        @*<ul class="navbar-nav">
                <li class="nav-item">
                    @Html.ActionLink("Xin chào " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
                </li>
                <li class="nav-item">
                    <a href="javascript:document.getElementById('logoutForm').submit()" class="nav-link">Đăng xuất</a>
                </li>
            </ul>*@


<div class="dropdown text-end">
    <a href="#" class="d-block link-dark text-decoration-none dropdown-toggle show" id="dropdownUser1" data-bs-toggle="dropdown" aria-expanded="true">
        <img src="https://github.com/mdo.png" alt="@User.Identity.GetUserName()" width="32" height="32" class="rounded-circle">
    </a>
    <ul class="dropdown-menu text-small"
        aria-labelledby="dropdownUser1"
        style="position: absolute; inset: 0 auto auto 0; margin: 0; transform: translate(-112px, 34px);"
        data-popper-placement="bottom-end">
        <li>
            @Html.ActionLink(
                "Cài đặt",
                "Index",
                "Setting",
                routeValues: null,
                htmlAttributes: new { title = "Cài đặt", @class = "dropdown-item" }
            )
        </li>
        <li class="nav-item">
            @Html.ActionLink(
                "Xin chào " + User.Identity.GetUserName() + "!",
                "Index",
                "Manage",
                routeValues: null,
                htmlAttributes: new { title = "Manage", @class = "dropdown-item" }
            )
        </li>
        <li><hr class="dropdown-divider"></li>
        <li class="nav-item">
            <a href="javascript:document.getElementById('logoutForm').submit()"
               class="dropdown-item">
                Đăng xuất
            </a>
        </li>
    </ul>
</div>
    }
}
else
{
    <ul class="navbar-nav ms-auto">
        <li class="nav-item">
            @Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })
        </li>
        <li class="nav-item">
            @Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })
        </li>
    </ul>

}
